import { addons } from '@storybook/addons';

addons.setConfig({
    isFullscreen: false,
    showNav: true,
    showPanel: false,
    sidebarAnimations: false,
    enableShortcuts: true,
    isToolshown: false,
});
